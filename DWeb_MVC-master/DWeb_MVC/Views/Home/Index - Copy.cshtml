@{
    ViewBag.Title = "Home";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
        }

        .navbar {
            margin-bottom: 20px;
        }

        .search-bar {
            margin-top: 20px;
        }

        .content {
            text-align: center;
            margin-top: 50px;
        }

            .content h1 {
                font-size: 3rem;
                font-weight: bold;
            }

            .content .list-group a {
                font-size: 1.2rem;
                color: #007bff;
            }

                .content .list-group a:hover {
                    text-decoration: none;
                    color: #0056b3;
                }

        .suggestions {
            z-index: 1000;
            width: 100%;
            background: white;
            border: 1px solid #ccc;
            border-radius: 0 0 4px 4px;
            max-height: 200px;
            overflow-y: auto;
            padding: 0;
            margin-top: -1px; /* Ensure the dropdown is right below the input field */
        }

            .suggestions li {
                padding: 10px;
                cursor: pointer;
                list-style: none; /* Remove list markers */
            }

                .suggestions li:hover {
                    background-color: #f8f9fa;
                }

        .input-group {
            position: relative; /* Ensure the input group is the reference for absolute positioning */
            width: 100%;
        }
    </style>
</head>
<body>
    <!-- Barra de Pesquisa -->
    @* <div class="container search-bar">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="input-group mb-3">
                    <input id="inputsearch" name="inputsearch" type="text" class="form-control" placeholder="Pesquisar produtos..." aria-label="Pesquisar produtos">
                </div>
                <ul id="listProd" class="suggestions" style="display: none;"></ul>
                <div id="search-results"></div> <!-- Container para resultados da busca -->
            </div>
        </div>
    </div> *@

    <!-- Conteúdo Principal -->
    <div class="container content">
        <h1>Gold Stirrup</h1>
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="list-group">
                    <a asp-controller="Produtos" asp-action="Index" class="list-group-item list-group-item-action">Ver catálogo</a>
                    <a asp-controller="Categorias" asp-action="Index" class="list-group-item list-group-item-action">Ver categorias</a>
                    <a asp-controller="Clientes" asp-action="Index" class="list-group-item list-group-item-action">Ver utilizadores</a>
                    <a asp-controller="Compras" asp-action="Index" class="list-group-item list-group-item-action">Ver compras</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Sobre nos -->
    <div class="container content">
        <div class="divContatoEmpresa">
            <h2>Informações de contacto</h2>
            <p>Morada: Rua cavaleiros de cristo, 34, Tomar</p>
            <p>Número de telefone: +351 938294618</p>
            <p>Email: info@goldsaddle.pt</p>
        </div>
    </div>


    <script>
                @*
        var products = @Html.Raw(Json.Serialize(Model.products));

        function updateProductList(products, input) {
            if (!Array.isArray(products)) {
                console.error('Products is not an array:', products);
                return;
            }

            const productList = document.getElementById('listProd');
            productList.innerHTML = ''; Limpa itens existentes
            if (input === "") {
                productList.style.display = 'none';
                return;
            }
            products.forEach(product => {
                if (product.Nome.toLowerCase().includes(input.toLowerCase())) { Corrigido para 'Nome'
                    const li = document.createElement('li');
                    li.onclick = function () {
                        location.href = "/Produtos/Details/" + product.Id; Corrigido para 'Id'
                    }
                    li.textContent = product.Nome; Corrigido para 'Nome'
                    productList.appendChild(li);
                }
            });
            productList.style.display = productList.children.length > 0 ? 'block' : 'none';
        }

        window.onload = function () {
            const searchInput = document.getElementById('inputsearch');
            searchInput.addEventListener('input', function () {
                updateProductList(products, searchInput.value);
            });*@
        };
        // var btnsearch = document.getElementById("search");
        // btnsearch.addEventListener("click", async (e) => {
        //     var inputsearch = document.getElementById("inputsearch");
        //     const response = await fetch(`/Produtos/Search/`, {
        //         method: 'GET',

        //     });

        //     if (response.ok) {
        //         const html = await response.text();
        //         document.getElementById('search-results').innerHTML = html;
        //     } else {
        //         console.error('HTTP error:', response.status);
        //     }
        // });
    </script>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>